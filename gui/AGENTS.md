# Production GUI Task Rule

Before inspecting, editing, or verifying any file in `gui/`, MUST read `LESSONS_MAP.md` in this directory. The repository-root lesson map is not a substitute: this map owns production GUI authority, product grammar, design taste, scene/GDScript/Rust format, promotion gates, and native proof.

If the task translates a behavior discovered in `../gui-prototype/`, also read `../gui-prototype/LESSONS_MAP.md`. Treat that behavior as a candidate until the production contract record, native implementation, and rendered Godot proof all exist.
