//! The sleep write: a spirit casts one boat into the Sea.
//!
//! Skeleton only. Each function names its absorb target from the
//! 2026-08-23 census. Bodies arrive with the extraction quests.

/// Write one boat as a durable memory and read back its outbox pointer.
/// Absorbs house-substrate/src/paper_boat.rs:18 `paper_boat_sleep`.
pub fn sleep() {
    todo!("extraction: absorbs paper_boat.rs:18")
}

/// Insert the boat row with source-path idempotency inside one transaction.
/// Absorbs the boat branch of house-substrate/src/remember.rs:448.
pub fn write_boat_tx() {
    todo!("extraction: absorbs remember.rs:448 boat branch")
}

/// Read the `boat.ready` outbox pointer the 0016 trigger enqueued,
/// inside the same transaction. This is the one seam between the boat
/// shape and the crane shape.
/// Absorbs house-substrate/src/paper_boat.rs:56.
pub fn ready_pointer() {
    todo!("extraction: absorbs paper_boat.rs:56 seam query")
}
