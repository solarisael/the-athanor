[package]
name = "presence-turn"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
presence = { path = "../presence" }
summoning = { path = "../summoning" }

[dev-dependencies]
presence-frame = { path = "../presence-frame" }

[lib]
name = "presence_turn"
path = "src/lib.rs"
