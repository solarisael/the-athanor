{
  "name": "solarisael-house",
  "version": "0.9.0",
  "description": "A persistent room, identity, continuity, and memory system for AI agents and collaborators.",
  "author": "Sol",
  "license": "Apache-2.0",
  "repository": "github:solarisael/solarisael-house",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "bun test && python -m unittest discover -s src -p \"test_*.py\"",
    "test:api": "bun test tests/api-contract.test.ts",
    "test:python": "python -m unittest discover -s src -p \"test_*.py\""
  }
}
