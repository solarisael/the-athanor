# Athanor GUI Prototype Lesson Map

The PostgreSQL lesson registry is authoritative. This file names lessons by number. Query them directly:

```json
{"type":"coding","ids":[9,10,11,12,14,27,134,143,194,196,200,316,317,321,322,328,340,381,382,383,384,385,388]}
{"type":"project","ids":[117,118,119,120,412,413]}
{"type":"design","ids":[293,294,295,296,297,298,299,300,301,302,303,304,409,422]}
```

IDs are scoped by family; always pass type. Direct lookups need an Athanor on or after 352b85e (dev/next, 2026-09-13). On an older build, lessons stay hidden from a bare query; supply languageKeys and technologyKeys from the repo's stack. Refreshed: 2026-09-13.

## Always

- Coding #9 — plain line, clean door, sharp refusal. Use for: implementing or proving prototype behavior.
- Coding #10 — names predict the answer or effect and do not lie. Use for: implementing or proving prototype behavior.
- Coding #11 — names predict the answer or effect and do not lie. Use for: implementing or proving prototype behavior.
- Coding #134 — frontend UX proof belongs to the rendered surface. Use for: implementing or proving prototype behavior.

## Lesson loadout

Retrieve these bodies before design or implementation work in this directory. Existing source wording is retained verbatim; grouped wording is repeated for each original ID, not asserted to be an individual registry title.

## Design — load together

- Design #293 — visual weight follows consequence. Use for: designing or changing visual states.
- Design #294 — unsafe compositions should be unconstructible. Use for: designing or changing visual states.
- Design #295 — unavailable controls state why. Use for: designing or changing visual states.
- Design #296 — authority, relevance, chronology, and health use separate channels. Use for: designing or changing visual states.
- Design #297 — fixed disclosures cannot be softened; plausible fake content is refused. Use for: designing or changing visual states.
- Design #298 — stable color and type roles belong to tokens. Use for: designing or changing visual states.
- Design #299 — one root owns global visual state; independent axes remain independent. Use for: designing or changing visual states.
- Design #300 — effects are spend; quiet is the carrier. Use for: designing or changing visual states.
- Design #301 — state survives grayscale and stillness. Use for: designing or changing visual states.
- Design #302 — required text meets measured contrast. Use for: designing or changing visual states.
- Design #303 — component, existing class, then local layout glue. Use for: designing or changing visual states.
- Design #304 — product vocabulary composes canon and never leaks into protocol authority. Use for: designing or changing visual states.

## Athanor product

- Project #117 — combine familiar agent interaction, Solarisael visuals, and Athanor domain meaning. Use for: changing product objects or authority boundaries.
- Project #118 — mapping, provenance, ambiguity, and durable effects remain inspectable. Use for: changing product objects or authority boundaries.
- Project #119 — authored effects, local surfaces, and environmental overlays remain independent. Use for: changing product objects or authority boundaries.
- Project #120 — preserve machine-legible contracts and human authority/correction. Use for: changing product objects or authority boundaries.

## Kitten dispatch — load before every fanout

- Coding #316 — execution has zero inference budget; a missing path produces a halt and question, never invented continuation. Use for: preparing any bounded fanout.
- Coding #317 — write each bounded quest in the A Squall register, keep its own words under 350, and deliver relevant lesson bodies rather than bare IDs. Use for: preparing any bounded fanout.
- Coding #322 — name `C:/Projects/the-athanor` as the canonical execution root when dispatch begins from a room outside the project. Use for: preparing any bounded fanout.
- Coding #328 — census shared terrain once, then give editing kittens exact disjoint coordinates and a fixed cross-task contract. Use for: preparing any bounded fanout.
- Coding #340 — subagents are kittens; name them, explain why the work matters, offer bounded autonomy and refusal, speak with affection, and receive their care warmly regardless of outcome. Use for: preparing any bounded fanout.

## Code and proof

- Coding #12 — each file has one center of gravity. Use for: implementing or proving prototype behavior.
- Coding #14 — code makes its refusals and loyalties visible. Use for: implementing or proving prototype behavior.
- Coding #27 — write the honest first shape, then compress without losing truth. Use for: implementing or proving prototype behavior.
- Coding #143 — centralize only semantic behavior with shared authority. Use for: implementing or proving prototype behavior.
- Coding #194 — comments preserve intent, danger, ritual, and unrecoverable constraints. Use for: implementing or proving prototype behavior.
- Coding #196 — delete clever compression when the plain line reads better. Use for: implementing or proving prototype behavior.
- Coding #200 — negative space punctuates completed thoughts. Use for: implementing or proving prototype behavior.
- Coding #321 — keep the operator feedback loop to one bounded change and immediate real-surface proof. Use for: implementing or proving prototype behavior.
- Coding #381 — formatter floor, mental-operation line breaks, ownership-shaped indentation, stable alignment, and one top-level scan path. Use for: implementing or proving prototype behavior.
- Coding #382 — formatter floor, mental-operation line breaks, ownership-shaped indentation, stable alignment, and one top-level scan path. Use for: implementing or proving prototype behavior.
- Coding #383 — formatter floor, mental-operation line breaks, ownership-shaped indentation, stable alignment, and one top-level scan path. Use for: implementing or proving prototype behavior.
- Coding #384 — formatter floor, mental-operation line breaks, ownership-shaped indentation, stable alignment, and one top-level scan path. Use for: implementing or proving prototype behavior.
- Coding #385 — formatter floor, mental-operation line breaks, ownership-shaped indentation, stable alignment, and one top-level scan path. Use for: implementing or proving prototype behavior.
- Coding #388 — load its full body before any CSS formatting or organization sweep; route the later bounded reformat through this lesson rather than mixing it into feature work. Use for: implementing or proving prototype behavior.

## Prose references

- Project #412 — (title in registry). Use for: mechanical-fact discoverability.
- Project #413 — (title in registry). Use for: replacing the local mechanical snapshot with the typed Host/substrate snapshot.
- Design #409 — (title in registry). Use for: forced-colors-safe focus treatment.
- Design #422 — (title in registry). Use for: replacing stale connection-apology captions with truthful source state.

## Untyped reference

- Lesson #402 (family unspecified) — Refusals and receipts must own their lifecycle boundaries. Use for: recovering the historical durable-shelf search reference.

## Uncertainties

- Project #412, Project #413, Design #409, and Design #422 have no title in the original map; none is invented here.
- Coding #10 and Coding #11 share one source description; Coding #381–385 share another. Their individual registry titles are not supplied.
- The historical search query `lesson 402` returns `Refusals and receipts must own their lifecycle boundaries`, but the map does not identify its family. Retain this untyped reference and exact title; do not guess a typed lookup.
- Existing lesson-line descriptions are retained as supplied; their status as exact PostgreSQL titles is not established by this rewrite.

## Repo facts without a lesson number

- Active checkout: `C:/Projects/the-athanor-dev/the-athanor`; never use a second clone.
- Coding #322 retains its historical root wording, not the active checkout root.
- Scope: local repo contracts and historical receipts, not lesson bodies or new verification claims.
- Historical boundary: dated receipts describe their own wave, not a fresh deployment claim.
- Governed path: `gui-prototype/`, the deliberately cheap HTML/CSS/JavaScript interaction laboratory for The Athanor GUI.
- Excluded scope: repository root, Host, substrate, and production Godot client.
- Production translation map: `../gui/LESSONS_MAP.md`.
- Authority: PostgreSQL remains authoritative for lesson and memory bodies.
- Local routing content: product rulings, design taste, code grammar, promotion gates, and proof.
- Before consequential work, retrieve named lesson bodies; bare IDs are routing labels, not delivery.

### Purpose and boundary

- Purpose: answer interaction questions while they remain cheap.
- Question: what does the operator naturally reach for?
- Question: which objects belong in navigation?
- Question: what changes when an entity is selected?
- Question: which information must remain visible while the center instrument changes?
- Question: how do drawers, focus, keyboard movement, density, and responsive geometry feel?
- Implementation: native ES modules with real imports, no framework, and no build step.
- Operator ruling, 2026-08-21: instrument centers of gravity get their own module before the monolith becomes load-bearing.
- `index.html`: semantic shell and stable interaction anatomy using `<script type="module">`.
- `styles.css`: exploratory geometry, hierarchy, state, and responsive treatment.
- `app.js`: interaction shell containing fixture data, one explicit state object, render waist, transitions, and listeners.
- `board/index.js`: Docket instrument containing live quest board, room Hallway inbox, evidence drawers, and its own source state.
- `board/index.js`: gives the mantle Bell the inbox round it already read.
- `board/hallway-messages.js`: Hallway messages door with one lazy drawer per Hallway row, one round per hallway key, and escape for peer prose.
- `sediment/index.js`: durable instrument containing live memory and lesson timelines, full memory reads, keyset pagination, and per-shelf source state.
- `pulse.js`: Insula instrument containing stamped snapshot, live-wire transport, derivation, render, and its own source state.
- `mechanics.js`: observatory containing source-census snapshot, category/query/scroll view state, and its renders.
- `text.js`: shared text safety through `escapeHtml`.
- `serve.ts`: local proxy owning Host access and exposing named Host routes, including Chat submission and repair.
- Security: bearer token stays server-side.
- Instrument modules own local state and expose narrow doors.
- `init...`: shell injects `requestRender`/DOM handles once at boot.
- `handle...Click`/`handle...Input`: returns whether the instrument owned the event.
- Instrument modules expose their `render...` functions.
- Shell ownership: waists, global state object, and listeners.
- Extraction seams: Bell, switcher, drawer, and composer each move to their own module when next materially touched, never in a big-bang sweep.
- Chat submits operator messages to the connected room and reads its message ring.
- Chat does not provide persisted session history or session creation.
- Other instruments distinguish live reads, unavailable data, and dated snapshots.
- Prototype syntax does not enter production through copy-and-paste.
- Public Pages builder emits a separate static specimen.
- Public Pages builder removes private telemetry and operator configuration and sanitizes every emitted app script.
- Public Pages has no Host connection, model calls, delivery, or persistence.
- Public Pages visible disclosure states these limits.

### Current product grammar

- Primary modes are exactly **Direct**, **Hallways**, and **Projects**.
- **Direct**: one collection of agent conversations.
- Direct rows contain agent identity, image or glyph, presence, latest message, and time.
- **Hallways**: a record of *gatherings*, dated group conversations where agents meet without merging rooms or identities.
- Hallways mode lists threads (gatherings) recent-first.
- Each thread row carries its date, hallway badge, and observed participants.
- Durable membership and full thread history live on the hallway record, one click behind the thread.
- Hallway is a record surface, never a navigation floor.
- Threads end through an honored explicit **seal** or janitorial inactivity fold, always visibly marked as automatic.
- Membership means who may enter and is a hallway fact.
- Participation means who actually spoke and is an observed thread fact.
- Membership and participation never merge.
- **Projects**: the `This House` Docket subject.
- Host board does not report separate projects or project scope.
- Selecting an entity changes the center instrument.
- Compact workspace strip names only the active mode because the entity owns its own header.
- Refusal: no workspace tab per spirit.
- Refusal: no merged room created to simulate a Hallway.
- Refusal: no archive labels such as `S01–S14` or `R01` used as product ontology.
- Refusal: no giant screen taxonomy before ordinary navigation and selection feel right.
- Refusal: no second navigation concept duplicating Direct, Hallways, or Projects under different names.
- Refusal: no hallway-as-container navigation; a hallway with nested threads would be a Project wearing a social costume.
- Operator ruling, 2026-08-17, memory #3639: Projects remain the only container mode.
- Refusal: no infinite-context default hallway; every hallway conversation is a dated thread with an end state.
- Current ruling sources: House memories #3555, #3572, and #3639, threads-lead ontology, 2026-08-17.
- A later operator ruling may change the current ruling; an older mockup or archive may not.

#### The three-layer slot model (2026-08-17 ruling, second wave)

- Every subject exposes exactly **three view slots**, positional keys `1`–`3`, with one semantic layer each.

| slot | layer | direct | hallway | project | house |
|---|---|---|---|---|---|
| 1 | **live** — where you stand and speak | Chat | Thread | Overview | Overview |
| 2 | **state** — machinery underneath | Status | Status | Status | Mechanics |
| 3 | **durable** — dated timeline, newest first | Memories | Record | Evidence | Memories & Lessons |

- Durable layer represents records that survive live work.
- Slot model does not imply every write action exists.
- Chat does not offer memory or boat writes.
- Hallway sealing and folding remain unavailable.
- Slot 3 reads dated records through its named Host routes.
- Collapse consequence: Direct slot 1 is **Chat**, showing the connected room's message ring, not persisted session history.
- Disconnected Direct has no invented messages.
- Former session menu and creation callbacks are removed.
- New session and New spirit remain visibly unavailable in the switcher.
- Session, model, and Presence provenance remain `Not reported` when the Host does not supply them.
- `Context` and `Substrate` merge into the state slot.
- Hallway `Actions` view dissolves; action events stay inline in the thread timeline.
- House slot 2 owns the Mechanical observatory.
- Account Settings owns local interface controls and provides a stable direct door into House mechanics.
- House, spirit, hallway, and project state keep separate authority even when their rows resemble one another.
- Durable slot is memory search's landing surface; switcher results route to slot 3 of the owning subject.

### Composition

- Shell has four stable regions.
- **Left collection:** mode switcher, current collection, and operator/account entry.
- **Center instrument:** selected conversation or project, its header, timeline or working surface, and primary composer/action.
- **Context inspector:** selected entity or selected item details, never a duplicate center surface.
- **Bottom status strip:** quiet, glanceable channels that expand only when asked.
- Deeper instruments grow by composing the same objects.
- Agent dashboards, Recall, GIGA, health, sources, canon, memory, and kitten work do not each demand a new global navigation axis.

### Design taste

#### Familiar first, specific second

- Use familiar agent-application grammar for navigation, conversations, selection, drawers, inspectors, and composition.
- Let Solarisael identity enter through typography, surfaces, spacing, texture, motion, and exact language.
- Do not paste an editorial website into a chat shell.
- Do not flatten the House into generic SaaS chrome.

#### Quiet carries weight

- Ordinary surface stays quiet.
- Spend emphasis only where consequence, selection, refusal, or live state earns it.
- Decorative boxes, helper copy, badges, and effects do not multiply merely because there is room.

#### Structure survives grayscale and stillness

- Prototype is deliberately grayscale.
- Hierarchy must use anatomy, spacing, border weight, type, marks, and plain words before color or motion helps.
- Selection, presence, authority, and health never depend on hue alone.
- Reduced motion must preserve a complete static state.

#### Copy belongs to the operator's world

- Use names the operator would naturally say: `Direct`, `Hallways`, `Projects`, `Settings`, `Account`, `Send`.
- Remove prose explaining the interface merely because the interface exists.
- Implementation-stage words such as `prototype`, `fixture`, `demo`, and `candidate` never enter product chrome.
- Honesty comes from truthful offline or unavailable states plus one isolated About disclosure.
- Proposals, absent contracts, and non-authoritative effects still expose fixed copy at the exact boundary where confusion is possible.

#### Weight follows consequence

- Control weight comes from its action: durable, destructive, safe, or cancel.
- Disabled or unavailable controls state why.
- Do not expose free-form `primary`, `variant`, or emphasis choices that let appearance lie about consequence.

#### Alignment reveals relationships

- Align labels, selects, checkboxes, dates, and status channels when comparison is real.
- Do not create ornamental columns whose whitespace explodes on the next copy edit.
- Dense does not mean cramped; compact surfaces still need a clean scan path.

### Interaction contracts

#### Selection

- Changing modes selects a valid entity in that mode.
- Selecting an entity updates list state, center header and content, inspector, and mode label as one transition.
- Selected message state is subordinate to the selected entity and clears when the entity changes.
- Dynamic text is escaped before entering generated HTML.

#### Hallway presence and collaboration candidates

- Status: Hallway collaboration shell is a prototype candidate, not production architecture.
- Requirement: make invisible authority visible without importing a host/guest topology.
- Authority and identity remain separate axes.
- Hallway record and durable membership are PostgreSQL-authoritative and belong to no participant.
- Operator, room, spirit, embodied session, permission, connection state, and read position remain separately named facts.
- One spirit may have several concurrent embodied sessions.
- An avatar or spirit name never identifies a presence by itself.
- Presence exposes a stable session distinction suitable for selection without making an opaque identifier the primary human label.
- `present`, `embodied`, `currently reading`, and `has read through` are different claims.
- A stale read cursor never implies absence, neglect, attention, or social obligation.
- First specimen: title, durable access state, and connection/liveness state occupy separate visible channels.
- First specimen: presence strip shows spirit, stable session distinction, and explicit observer/participant permission.
- First specimen: immediate durable snapshot precedes an explicit catch-up/live boundary.
- First specimen: human and spirit messages appear in the quiet timeline.
- First specimen: collapsed technical and artifact events appear only when belonging to the shared Hallway record.
- First specimen: selected-presence inspector shows room, spirit, session, permission, liveness, activity, and carefully labeled session state.
- First specimen: permission-aware composer visibly distinguishes watching from participating before input.
- Liveness uses words, not only dots: `connected`, `reconnecting`, `stale`, and `ended`.
- Message delivery or observation never silently wakes or summons a spirit.
- Exact read receipts are optional operational facts, not the default social surface.
- Prefer neutral `caught up through 09:52` or `cursor not current` wording without implying why.

#### Thread view candidate (2026-08-17 ruling)

- Hallways mode selects a thread, never a hallway.
- Thread header carries thread title, date, hallway badge, and only *live* channels: connection and delivery.
- Constant authority and access facts move to the inspector.
- Chrome that never changes is furniture, never header.
- Thread right region is a full-height hallway member column using the Discord shape.
- Member column groups by spirit with embodied-session sub-rows and liveness in words.
- Member column contains members only; the record is not a dock face.
- Thread end state is `open`, `sealed`, or `folded`.
- `sealed` is an explicit, honored act.
- `folded` comes from the inactivity janitor and is visibly marked automatic.
- A sealed or folded thread renders its closing line where the catch-up boundary would sit.
- A sealed or folded thread's composer states why it refuses input.
- Hallway record occupies the thread's **durable slot** (slot 3).
- Durable slot places the membership card above seals, folds, and durable artifacts as dated timeline entries, newest first.
- Hallway record is a record surface with no navigation ambitions.
- Header hallway badge routes to the hallway record.

#### Action ledger candidate

- An action ledger is not a thought transcript.
- Tool and recall events belong inline in every conversation timeline as collapsed rows.
- Morning-check grammar comes from operator ruling 2026-08-17.
- Collapsed event fields: sanitized verb, target, state, and time.
- Expansion may show declared intent, permission-filtered arguments, observable result, errors, produced artifacts, and evidence, including the full recall card.
- Refusal: no raw private reasoning.
- Refusal: no unsanitized tokens, secrets, private paths, user content, or credentials.
- Refusal: no tool result presented as authority beyond its actual receipt.
- Refusal: no auto-expanded technical noise.
- Inline events arrive collapsed, one row per event, never a raw ledger dump drowning the conversation.

#### Direct working-session candidate

- Direct owns the conversation with a spirit.
- A live work stream belongs to one selected embodied session.
- Selecting a spirit may offer its conversation and a collection of working sessions.
- Spirit selection may not pour tool traffic into the conversation or silently choose the most recently registered embodiment.
- Prototype gate: working-session instrument must identify which embodiment is being watched.
- Prototype gate: working-session instrument must identify who authorized observation.
- Prototype gate: working-session instrument must state whether delivery is live, delayed, stale, or ended.
- Prototype gate: working-session instrument must identify which events are redacted.
- Prototype gate: working-session instrument must state whether observation changes or wakes the session.
- Prototype gate: working-session instrument must state what persists after the session closes.
- Session pressure remains outside the ordinary Direct header.
- When relevant, selected-session inspector labels channels separately: `Context used`, `Compaction`, `Recall`, and `Evidence`.
- A naked percentage or one combined green gauge is refused.

#### Observer invitation candidate

- A capability link may request bounded admission but never becomes durable Hallway authority.
- Future invitations require expiry, revocation, requested permission, authenticated-presence exchange, durable admission audit, and no observer-to-participant escalation.

#### Nested sidebar drawer

- Drawer is one ordered set of rendered panes.
- Drawer discovers panes through `data-drawer-pane`; JavaScript does not maintain a second pane inventory.
- Exactly one pane is `active`; earlier panes are `before`, later panes `after`.
- Inactive panes are both `aria-hidden="true"` and `inert`.
- Opening records the trigger that should receive focus on return.
- Back and Escape move exactly one state outward.
- At mobile root, Escape closes the visible sidebar and returns focus to its toggle before a later Escape may leave House.
- Focus waits for incoming-pane animation to settle and uses `preventScroll`.
- A generation token guards focus so stale work cannot steal focus after rapid navigation.
- Drawer and track use `overflow: clip`, not `overflow: hidden`.
- Chromium may horizontally scroll a hidden overflow container when focus enters a still-transformed pane, throwing the drawer offscreen.
- House memories #3565 and #3566 carry the discovery and repair.

#### House mechanical observatory

- House slot 2 is the mechanical observatory.
- Account → Settings provides a stable direct door through `navigateToSubjectView("house", "state")`.
- Account → Settings creates neither a fourth global mode nor a second settings owner.
- Disconnected surface renders one local `HOUSE_MECHANICS_SNAPSHOT` from the current source census.
- Seven categories: Recall & Context; Memory, Lessons & Anamnesis; GIGA & Embeddings; Host, Delivery & Hallways; Rooms & Sessions; Backups; Advanced Guardrails.
- Every row carries effective value, default, scope, owner, mutability, secrecy, apply mode, health, and consequence.
- Category buttons filter locally and clear the search.
- Search always crosses every category.
- Search updates only result/status containers, preserving input focus.
- Native `details` rows disclose typed metadata without adding another panel.
- Status header shows the same Host link chip that Pulse shows.
- Status header places the chip beside source-census revision, disconnected surface, and read-only authority.
- PostgreSQL-backed rows may describe future `Host-writable` authority while exposing no mutation control in the disconnected surface.
- Secret-bearing configuration exposes presence or health only.
- When Host connection lands, replace the local snapshot with the typed Host/substrate snapshot from project lesson #413.
- When Host connection lands, delete duplicated client facts.
- Project lesson #412 owns the discoverability standard: a buried mechanical fact is absent from the operator's world.
- House Overview, Mechanics, and Memories & Lessons share one `1040px` outer canvas.
- Internal content may choose a narrower reading measure when a long body needs it.
- Slot transition must not make the House frame jump.

#### Responsive behavior

- Desktop exposes collection, center, and inspector when space permits.
- Intermediate width removes or overlays the inspector before crushing the center instrument.
- Mobile uses one readable center column with explicit collection and inspector doors.
- No viewport may acquire horizontal document overflow.
- Responsive behavior preserves identity and capability.
- Responsive behavior does not silently delete the only route to an operation.

#### Hallway Bell and read attention

- Bell is the global door into Hallway attention.
- Bell occupies no fourth navigation mode and owns no duplicate conversation surface.
- Bell lives in the mantle topbar because attention can arrive while the operator stands in Direct, Hallways, Projects, or House.
- Bell shows the live inbox round that `board/index.js` already read.
- Bell opens no door of its own.
- Ordinary unread is the Host `unread` count and renders as a round count.
- Explicit attention is the Host `mentions` count and renders as a squared, heavier count.
- Monochrome bell icon is the entire visual label.
- Ordinary count overlays the bell icon's upper-right edge.
- Targeted count overlays the bell icon's upper-left edge.
- Button's exact accessible label carries the full totals.
- Body-text parsing never creates recipient authority.
- An unasked door, a refused door, and a caught-up room are three different named states; none shows a row.
- A row names one Hallway.
- Row verb opens the Board, where the messages behind that Hallway open.
- Reading the Bell acknowledges nothing: no cursor moves and no Bell row clears.
- Bell scope belongs to the authenticated room/spirit presence.
- Selecting a Direct subject never impersonates that spirit.
- Hallway and future Project notifications share one typed global inbox.
- Inbox rows and tabs retain contextual badges.
- An explicit embodied room/spirit identity switch may replace the Bell scope.
- Modal layer lives outside `.app-shell`.
- While modal is open, shell is `inert` and focus enters the first route.
- On modal close, focus returns to the invoking control.
- Mobile sidebar follows the same rule at rest.
- When translated offscreen, mobile sidebar is also `aria-hidden` and `inert`.
- Invisible navigation cannot remain in the keyboard or accessibility tree.
- Bell rows are live Host reads.
- Unread marks on fixture Hallway threads in the list stay local fixtures; no door clears them.
- Footer and About surface preserve the non-authoritative boundary.

#### Status strip

- Status channels remain separate.
- Host transport, Recall mode, body, kittens, and delivery do not collapse into one green or red verdict.
- Expanding one channel explains that channel without becoming a dashboard modal.
- Local records remain simulated and non-authoritative.
- Status channels report disconnected surface truth through the value, receipt, refusal, or unavailable action that owns it.
- Isolated About door carries the global experiment boundary.
- Published screenshots and handoffs add disclosure outside the product anatomy.
- Full-width hint and census banners have no interface owner and are deleted.

### HTML grammar

- Prefer semantic landmarks and native controls: `main`, `aside`, `nav`, `header`, `section`, `article`, `form`, `button`, `textarea`, and `select`.
- Stable structure lives in HTML.
- JavaScript renders changing collections and selected content into named containers.
- Every icon-only control has an accessible name.
- Visually reflected state also appears through `aria-pressed`, `aria-expanded`, `aria-hidden`, `inert`, or ordinary text as appropriate.
- `data-*` attributes name behavior and state.
- Classes name styling.
- Do not overload `data-*` attributes and classes as each other.
- Keep heading order, labels, focusability, and DOM order coherent without CSS.
- Do not add wrapper elements unless they own layout, semantics, clipping, or a transition boundary.

### CSS grammar

- Keep one local exploratory palette and metric vocabulary near the top of `styles.css`.
- Do not spread new literal values through component rules.
- Local variables are experiment vocabulary, not Solarisael canon.
- Rule order: reset and local tokens → shell geometry → stable regions → components → interaction states → responsive adaptations → reduced motion.
- Component selectors name visible anatomy.
- State selectors use explicit classes, ARIA attributes, or `data-*` state already owned by the element.
- Prefer grid for shell geometry and real two-axis relationships.
- Prefer flex for one-dimensional component flow.
- Every grid or flex child that may shrink owns the necessary `min-width: 0` or `min-height: 0`.
- Use `overflow: clip` for purely visual clipping.
- Use scroll containers only where scrolling is intentional and keyboard/focus behavior has been exercised.
- Transitions are short, local, and subordinate to reading.
- `prefers-reduced-motion` removes transitions without erasing state.
- No inline style glue while the three-file experiment can state the rule clearly in `styles.css`.
- No framework or utility vocabulary without an actual implementation behind the class name.
- Current debt: the prototype already contains repeated literal grayscale values.
- Do not expand literal grayscale scatter.
- Consolidate a value only when its semantic role is clear.
- Do not pretend exploratory variables are final design tokens.

### JavaScript grammar

- Keep one obvious downward scan in the following order.
- Scan 1: fixture records.
- Scan 2: display labels and fixed detail data.
- Scan 3: one explicit state object.
- Scan 4: stable DOM handles.
- Scan 5: pure lookup and rendering helpers.
- Scan 6: named state transitions.
- Scan 7: event listeners.
- Scan 8: responsive boot and initial render.
- Names predict effects: `openConversation`, `setDrawerView`, `returnDrawerView`, `setInspector`.
- One state transition owns each coordinated visual change.
- Event listeners delegate to transitions instead of editing unrelated DOM fragments ad hoc.
- Booleans and data attributes read as propositions or admitted states.
- Use event delegation for generated collections.
- Do not attach a listener per fixture row after each render.
- Escape fixture or user-authored text before interpolation.
- Never interpolate executable markup from conversation data.
- Keep transient UI state local.
- Do not imitate Host, persistence, transport, or domain authority in browser state.
- Comments preserve the reason for a non-obvious boundary or browser repair.
- Comments do not narrate the next statement.
- Expand the plain operation before inventing a helper.
- A helper earns itself by owning one transition, policy, or repeated semantic contract.
- Split `app.js` only when independent centers of gravity emerge.
- File length alone is not a seam.
- The main hand keeps intent, integration, taste, and rendered proof.
- A malformed dispatch is cancelled rather than accepted because its output happens to look useful.

### Promotion gate

- A prototype discovery enters `gui/` only when all six conditions below are true.
- Condition 1: Sol has judged the interaction on the rendered surface.
- Condition 2: the product object and owner are named without archive taxonomy.
- Condition 3: anatomy, states, refusals, focus, keyboard behavior, responsive behavior, and disclosure are explicit.
- Condition 4: the production map and `../gui/DESIGN_CONTRACTS.md` have a semantic home for it.
- Condition 5: Godot receives the behavior as a native scene/component contract.
- Condition 5 boundary: HTML, CSS, and JavaScript syntax are not copied across platforms.
- Condition 6: Host-owned data and effects remain Host-owned.
- If a decision is still being felt out, leave it here.
- Cheap cardboard is allowed to stay cardboard.

### Proof before calling a prototype change done

- Exercise the actual browser surface, not only source or an HTTP response.
- Required exercise: desktop geometry at the intended working viewport.
- Required exercise: narrow/mobile geometry at approximately 390 px.
- Required result: no horizontal document overflow.
- Required exercise: mouse selection across Direct, Hallways, and Projects.
- Required exercise: Bell open.
- Required exercise: separate unread/attention counts.
- Required exercise: exact thread routing.
- Required exercise: covered acknowledgment.
- Required exercise: structured recipient marker.
- Required result: narrow Hallway content ends before the fixed member dock, including recipient metadata.
- Required exercise: keyboard activation and visible focus.
- Required exercise: drawer root → Account → Settings → Back and Escape.
- Required exercise: rapid repeated drawer cycles without stale focus or offscreen geometry.
- Required exercise: inspector open/close and focus-safe selection.
- Required exercise: composer submission and refusal behavior under fixture rules.
- Required exercise: reduced-motion behavior.
- Required artifact: shared screenshots carrying an honest experiment disclosure.
- The proof receipt names the viewport, path exercised, and observed result.
- `It loaded` is not a UX receipt.

#### Proof receipt — 2026-08-18 Hallway Bell wave

- Historical setup: Chromium at `1440 × 1000`.
- `1440 × 1000`:
  - Direct, Hallways, and Projects selection exercised.
  - three-slot preservation exercised.
  - Bell open and focus entry exercised.
  - exact Morning check routing exercised.
  - counts changed from `3 unread / 1 attention` to `2 unread / 0 attention`.
  - recipient marker exercised.
  - composer submission exercised.
  - sealed-thread refusal exercised.
  - Account → Settings → Back/Escape exercised.
  - three rapid drawer cycles exercised.
  - inspector close/open exercised.
  - House Switcher exercised.
  - reduced motion exercised.
  - zero horizontal document overflow.
  - zero console or page errors.
- Historical setup: Chromium at `390 × 844`.
- `390 × 844`:
  - closed sidebar was absent from the accessibility tree and `inert`.
  - open/Escape restored the toggle.
  - Bell focus wrapped in both directions.
  - mode and slot keyboard chords preserved the selected slot.
  - Morning check routed to slot 1.
  - the active view ended at the fixed member dock boundary (`278 px`).
  - `To Kintsu room` remained visible.
  - zero horizontal document overflow.
  - zero console or page errors.
- Shared screenshots retain the Hallway inbox footer’s local-only disclosure.
- Proof boundary: no Host connection, PostgreSQL read cursor, durable Bell row, delivery, or persistence is claimed.

#### Proof receipt — 2026-08-18 mechanical observatory wave

- Historical setup: Chromium at `1440 × 1000`.
- `1440 × 1000`:
  - Account → Settings → Mechanical observatory routed to House slot 2.
  - 43 mechanisms appeared under eight category buttons including All.
  - the `timeout` query returned five exact rows across categories and retained input focus.
  - Backups cleared the query and returned three rows.
  - Backup retention disclosed default, scope, owner, apply mode, secrecy, and consequence.
  - zero mutation controls, horizontal overflow, console errors, or page errors.
- Historical setup: Chromium at `390 × 844`.
- `390 × 844`:
  - the Account door routed and returned the sidebar to `aria-hidden` plus `inert`.
  - the observatory occupied `364 px` inside a `390 px` viewport.
  - `backup` returned three all-category results with focus retained.
  - Rooms & Sessions returned six rows.
  - the expanded OMP model-default row remained within `13–377 px` and exposed `Next session`.
  - zero horizontal overflow, console errors, or page errors.
- Screenshots show the source-census revision, `Host offline`, and the disconnected read-only footer.
- Proof boundary: no Host snapshot, mutation command, PostgreSQL write, OMP connection, or secret value is claimed.

#### Proof receipt — 2026-08-18 GUI detail wave

- Historical setup: Chromium at `1440 × 1000`.
- `1440 × 1000`:
  - the topbar rendered an `18 × 18` two-path monochrome bell.
  - initial local state was `4 unread / 2 attention`.
  - the round unread count and squared two-pixel targeted count remained visually distinct.
  - Morning check acknowledged only returned message index 5 and left index 6 pending as `3 / 1`.
  - `@Kintsu` in body text produced no recipient marker.
  - structured `toRooms: ["kintsu"]` rendered `To Kintsu room`.
  - a sent local message rendered `Local-only · undelivered` with no Host/PostgreSQL receipt.
  - mechanics rows exposed `Effective`.
  - zero horizontal overflow, console errors, or page errors.
- Historical setup: Chromium at `390 × 844`.
- `390 × 844`:
  - the Bell icon remained visible while its text label hid.
  - House plus an open sidebar required two Escapes.
  - first Escape closed and re-inerted the visible sidebar while retaining House and returning focus.
  - second Escape restored Kintsu Direct.
  - mechanics rows stayed within `13–377 px`.
  - mechanics rows’ effective value ended at `371 px`.
  - the settled sidebar ended offscreen at `-5.8 px`.
  - zero horizontal overflow, console errors, or page errors.
- Current product chrome uses `Host offline`, `Source census`, `Local-only`, and `Disconnected surface` at their exact authority boundaries.
- Historical fixture/specimen terminology remains available in documentation and source-level class names only.

#### Proof receipt — 2026-08-18 Bell overlay and House width wave

- Historical setup: Chromium at `1440 × 1000`.
- `1440 × 1000`:
  - the Bell contained zero wording nodes.
  - the Bell button measured `44 × 43 px`.
  - the `18 × 18 px` icon remained centered.
  - targeted and unread counts sat at `y + 2 px` on opposite upper edges and ended above the icon bottom.
  - House Overview, Mechanics, and Memories & Lessons each occupied the same available `700 px` canvas.
  - zero horizontal overflow, console errors, or page errors.
- Historical setup: Chromium at `390 × 844`.
- `390 × 844`:
  - the icon-only Bell remained `44 × 43 px`.
  - both counts stayed overlaid at `y + 2 px`.
  - no document overflow appeared.
  - the screenshot preserves the exact button geometry and accessible label.
- Ordinary product chrome contains no `Bell` word.

#### Proof receipt — 2026-08-20 durable shelf search wave

- Historical setup: Chromium at `1440 × 1000`.
- `1440 × 1000`:
  - House slot 3 rendered `Search memories and lessons`, All/Memories/Lessons marks, and `10 entries`.
  - typing `lesson` filtered to `7 of 10 entries` with input focus retained.
  - the Lessons mark AND-composed with the query and re-pressed via `aria-pressed`.
  - the `hallway` query returned exactly one entry.
  - Ctrl+Space → `search memory` surfaced the live `Recall · Search memory · House › Memories & Lessons` command.
  - Enter closed the switcher, routed to House slot 3, and focus landed in the shelf search.
- Focus setup: double-rAF defer outlasts the shell's own scheduled focus.
- `1440 × 1000`:
  - Kodo's empty room shelf stated `No Kodo room memories available.` even mid-query.
  - switching subjects reset query and mark.
  - Tab from the input reached the mark buttons with a border-color focus treatment, forced-colors-safe per design #409.
  - zero horizontal overflow, console errors, or page errors.
- Historical setup: Chromium at `390 × 844`.
- `390 × 844`:
  - `.shelf-search` collapsed to one column and `.shelf-marks` wrapped via the shared mobile media block.
  - controls ended at `377 px` inside the `390 px` viewport.
  - first attempt overflowed to `393 px` and was repaired.
  - the multi-term query `lesson 402` returned exactly `1 of 10 entries` (`Refusals and receipts must own their lifecycle boundaries`).
  - focus retained.
  - zero horizontal overflow, console errors, or page errors.
- Search is term-AND over the visible row text: spoken date, raw stamp, title, mark, and detail.
- Search reuses the switcher grammar.
- Results update only `[data-durable-results]` and the status line, mechanics-style.
- Proof boundary: no Host connection, PostgreSQL query, or Recall transport is claimed.
- The shelves searched are the local durable fixtures.
- The counts state exactly what was searched.

#### Proof receipt — 2026-08-20 Insula Pulse wave

- Data provenance: every rendered number came from live PostgreSQL `insula` rows within one transaction stamped `2026-08-20 22:12 −03`, baked as `HOUSE_PULSE_SNAPSHOT`.
- Snapshot totals: 15,853 spans, 95,690,376 tokens in, 29 tool errors, and 192 refused projections.
- Snapshot receipts: real receipt ids included the day's `insula.backup` sha256.
- Proof boundary: no Host connection, live read, or auto-refresh was claimed.
- Provenance chips: `Host offline · PostgreSQL insula snapshot · Captured …`, per design #297.
- Caption removal: stale "Connect the Host to read…" apology captions were deleted per design #422.
- Lane anatomy: 20 lanes reused the `mechanics-row` species, design #303.
- Sampling: the ~2 s `knock_claim` doorman poll rendered as one aggregated lane per Tuner's read-side sampling law, hallway #124.
- Chromium at `1440 × 1000`:
  - House slot 2 rendered Insula · Pulse above the observatory.
  - four channels rendered Observations, Tokens, Loss, and Retention.
  - Retention showed the truthful `No rows expired · 13 sweep runs` state.
  - 20 lanes rendered per-class outcome flags.
  - 4 receipt kinds showed `Latest receipt per kind · 63 receipt points today`.
  - `tool_call` details disclosed `1,915 ok · 29 error · 15 cancelled`.
  - `tool_call` details disclosed max `24.6 min`, `tool_error 29 · session_shutdown 15`, and `insula.vitals.minute v1`.
  - the `insula.backup` receipt disclosed its full sha256.
  - observatory search returned its 3 `backup` rows below, retaining input focus and leaving all 24 pulse rows untouched.
  - keyboard focused the first lane summary, and Enter toggled it.
  - mechanics scroll retention held `900 → slot 3 → slot 2 → 900` exactly.
  - zero horizontal document overflow.
  - zero page errors.
- Chromium at `390 × 844`:
  - channel grid collapsed to one column.
  - lane and receipt summaries stacked.
  - the 64-hex receipt id ended at `331 px` inside the `390 px` viewport.
  - zero elements past the right edge.
  - zero horizontal document overflow.
  - zero page errors.
- Residual, not this wave's: `/site/fonts/InterVariable.woff2` 404s when the prototype is served standalone.
- Font provenance: the reference landed in `c7ad11f` with the Pages font work.
- Proof-harness residual, not a product regression: one retention mis-measure resulted from typing `backup32` into the still-focused search input.
- Retention re-test: clean after blur.
- Motion: none added; new sections were static anatomy.
- Wave scope: Pulse shipped without a vitals drilldown door.
- Drilldown boundary: trace/raw drilldown belonged to the Host-connected surface and its read routes, not a disconnected fixture.

#### Proof receipt — 2026-08-21 live wire and module split wave

- Architecture: the prototype became native ES modules.
- `pulse.js`: Insula instrument with pulse-local source state idle → pending → live | failed.
- `mechanics.js`: observatory with module-local category/query/scroll state.
- `text.js`: included in the native ES module split.
- `serve.ts`: static serving plus `/live/insula/vitals|retention` proxy to the kodo Host on :8788.
- Proxy security: bearer token server-side, POST-only, unknown routes and path traversal refused.
- Shell: shrank ~3,600 → ~2,880 lines and kept the waists.
- Query trigger: `ensurePulseQueried` fired from `openSubjectView`/`navigateToSubjectView` when House slot 2 opened, never from render.
- Chromium at `1440 × 1000`, LIVE:
  - entering House slot 2 auto-queried the Host.
  - rendered `Host connected · kodo Host · room-scoped vitals · Queried 00:55 local`.
  - 14 live lanes derived from real `insula.vitals.minute v1` rollups.
  - channels showed 6,290 settled events and 30,563,751 tokens in for the kodo room's last 24 h.
  - numbers grew between two queries, demonstrating genuinely live data.
  - `Query Host` re-queried on demand.
  - observatory search below returned its 3 `backup` rows with focus held.
  - a category click cleared the query through the module handler.
  - mechanics scroll retention held `700 → slot 3 → slot 2 → 700` through module state.
  - zero horizontal overflow.
  - zero page errors.
- FALLBACK setup: transport failure injected at the page's fetch boundary.
- FALLBACK result: surface used the stamped `2026-08-20 22:12 −03` snapshot.
- FALLBACK provenance: `Host unreachable · Failed to fetch` appeared beside snapshot provenance chips; live numbers were never faked.
- FALLBACK recovery: restoring transport and re-querying returned `Host connected`.
- Source-state proof: four source states, each visibly distinct.
- Chromium at `390 × 844`:
  - one-column channels.
  - live chips wrapped cleanly.
  - zero horizontal overflow.
  - zero page errors.
- Residual: standalone-serve font 404 remained, `c7ad11f`, Pages-owned.
- Residual: receipts block stayed snapshot-only until a Host route served receipt kinds.
- Residual: live lanes carried `raw rows only` for error classes because rollups did not carry them.

#### Proof receipt — 2026-08-24 Pulse rung 2

- Architecture: House slot 1 entered through `board/index.js`.
- Architecture: House and room slot 3 entered through `sediment/index.js`.
- Architecture: `app.js` kept the render and event waists.
- Proxy scope: `serve.ts` exposed only named POST routes for board, inbox, evidence, memory timeline/read, lesson timeline, and existing Insula reads.
- Proxy security: bearer token remained server-side.
- Entry behavior: entering House initiated the board read rather than leaving a truthful-but-idle panel behind.
- Chromium at `1440 × 1000`, LIVE setup: connected against the kodo Host on `:8788`.
- Chromium at `1440 × 1000`, LIVE:
  - board auto-read 19 quests and two Hallways.
  - rung-2 evidence drawer disclosed five acceptance criteria and two full receipts.
  - all seven inner ledger rows were collapsed first.
  - House slot 3 read 25 memories and 25 lessons.
  - House slot 3 retired every fixture row.
  - House slot 3 loaded both keyset timelines to 50 rows.
  - House slot 3 opened PostgreSQL memory `#3959` to its 2,841-character body.
- Cursor contract: lesson `updatedAt`, memory `createdAt`, both with numeric ids.
- Failure setup: forced refusals on board, inbox, evidence, memory timeline, lesson timeline, and memory read.
- Failure result: each forced refusal rendered the exact named reason and no invented live rows.
- Empty-state proof: forced empty arrays rendered named absence.
- Malformed-data proof: malformed timeline collections refused instead of masquerading as empty.
- Safety setup: hostile markup across every dynamic surface.
- Safety result: zero injected nodes and no script execution.
- Token proof boundary: actual bearer token absent from all 11 served prototype sources checked.
- Chromium at `390 × 844`:
  - durable surface occupied `364 px` inside the `390 px` viewport.
  - both timeline columns occupied `334 px` and collapsed to one column.
  - center scroller measured `690 / 6,181 px` client/scroll height.
  - zero horizontal document overflow.
- Desktop and narrow proof: no page errors.
- Console residual: pre-existing standalone font request remained the sole console 404.
- Focused contracts: `house-protocol` passed 49 tests.
- Focused contracts: `house-host --lib` passed 19 tests.
- Focused contracts: `panel_boundary_contract` passed its unauthenticated/no-database boundary test.
- Focused contracts: two dedicated-PostgreSQL cases were correctly ignored.
- Proof boundary: live browser round supplied PostgreSQL-backed proof; ignored tests deliberately did not borrow that proof from production.
- Authority gate: implementation and rendered evidence were complete.
- Authority gate: acceptance criterion 5 remained Sol's own squint-test verdict.
- Authority boundary: executor did not settle the operator's visual judgment on his behalf.

#### Proof receipt — 2026-09-05 live status strip and lane trace drawer

- Architecture: five hardcoded footer chips and three hardcoded Account state rows became one live round owned by new `health.js`.
- Health read: single `/live/health` read uses the `pulse.js` module shape, idle → pending → live | failed.
- Rendering: `renderStatusStrip` and `renderAccountState` share the round, preventing footer `Host ok` alongside drawer `Offline`.
- Proxy: `serve.ts` gained a method per allow-list entry and `/live/insula/trace`.
- Host contract: `/live/health` is the Host's only GET read and is called without a body.
- Security boundary: the bearer stays server-side, and the map remains the only reachable surface.
- Historical setup: Chromium at `1440 × 1000`, LIVE against the kodo Host on `:8787`.
- Strip result: `Host ok · kodo`, `Recall seq 2,739`, and `Delivery connected`, with steady dots on host and delivery.
- Popover result: Host schema version 1 and WebSocket path `/room/kodo/athanor/v1/ws`.
- Popover result: projection `recall_policy`, version/sequence `2,739`, state hash `2195a1fa3646…`, explicitly the recall policy cursor, **not** a count of memories.
- Popover result: `AKASHA enabled · broker configured · status connected · latest receipt event ad222f60-…-12dc7fceb375 · stream sequence 174`.
- Account result: `Surface = Live reads · writes none`, `Host = ok · kodo room`, and `Persistence = PostgreSQL ok`.
- Account insula result: `ok after 604 successful and 0 failed operations, at API schema 1`.
- Re-query result: popover `Query Host` transitioned through `Host querying…` back to `Host ok · kodo`.
- Contract absence: `body` renders `Body not reported by Host`; `kittens` renders `Kittens not reported by Host`.
- Absence explanation: no embodied-session or kitten field exists in the round; both channels enumerate the nine fields the Host answers.
- Failed-read behavior: both absence states persist because absence belongs to the health contract, not the round.
- Persistence boundary: nothing was upgraded; `Persistence` speaks only the Host's insula status word because the insula pool is the round's only persistence reading.
- Loss result, LIVE: `0 dropped · No writer drops · drop_count_sum summed to zero across 657 rollup rows`.
- Loss result without rollup rows: `Not known · No rollup rows in window · nothing to sum, so drops are unknown, not zero`.
- Prior loss wording: `0 dropped · writer-side drop receipts` did not disclose whether zero was measured.
- Trace proof: all five states rendered; clicking a lane opens one drawer.
- Missing-identity result: `insula.vitals.minute v1 rollups aggregate many spans and carry no trace id. The Host's trace route reads one exact trace id, and it serves no route that lists a lane's spans`.
- Historical trace boundary: missing identity was this surface's resting state; `query_trace` filters `WHERE trace_id=$2::uuid`, and no Host route enumerated traces.
- Trace setup: an identity supplied at the page boundary produced **real Host-served rows**.
- Trace result: `insula.trace v1 · trace c185225a-8f19-4370-9ec7-25e8caccbfc9`, 6 spans across `provider_request` (20.3 s), `tool_call` (190.5 ms), `provider_usage`, and `tool_result`.
- Trace row details: parent span ids and per-span outcome, error-class, and drop flags.
- Canonical-but-unknown trace result: `No spans · answered zero rows for trace 00000000-… inside this room's scope`.
- Non-canonical trace result: Host-authored `Trace refused · Host refused: invalid_request`.
- Hostile trace-id markup result: zero injected nodes and no script execution.
- Failure setup: transport failure injected at the page's fetch boundary, never by stopping `athanor.exe`, left the live House untouched.
- Failure result: `Host unreachable`, `Recall unreachable`, and `Delivery unreachable`, with attention dots.
- Failure Account result: `Surface = Local only · Host unreachable`.
- Failure source line: `Host unreachable · Failed to fetch`.
- Recovery result: restoring transport and re-querying returned every channel to live.
- Invalid-health result: a well-formed 200 carrying a non-health body was refused as `The health read failed: Host answered without a health status`.
- Invalid-health boundary: the non-health body was not rendered as a value.
- Rendered bugs: two bugs were found and fixed.
- Popover bug: clicking `Query Host` detached the button mid-dispatch, causing the document-level outside-click handler to classify its target outside the strip and close the popover.
- Popover fix: ignore a target already removed by this click; re-render the open popover each round rather than leaving stale content behind an updated chip.
- Lane-focus bug: opening a lane drawer lost keyboard focus to the panel re-render.
- Lane-focus fix: `renderWithLaneFocus` restores focus across a double frame.
- Lane-focus proof: four rapid cycles on one lane ended closed with focus still on `omp_adapter tool_call`.
- Historical viewport: Chromium at `390 × 844`.
- Compact chips: `Host ok`, `Recall 2,739`, `No body`, `No count`, and `Connected` replaced full text.
- Compact strip measurement: ended at `386 px` without scrolling.
- Compact Account measurement: ended at `275 px`, with its insula note wrapping.
- Compact trace measurement: drawer occupied `34–356 px`, with spans collapsed to one `292 px` column and widest inner element at `341 px`.
- Compact channels measurement: collapsed to one `334 px` column.
- Viewport results: zero horizontal document overflow and zero elements past the right edge at both viewports.
- Safety result: zero page errors at both viewports.
- Source inspection: bearer token and `runtime-secrets` appeared in none of the 11 served sources checked.
- Allow-list result: write route `/live/insula/events` was refused `404`.
- Method result: page `GET /live/health` was refused `405`; only the proxy converts the page's POST into the Host's GET.
- Historical residual: standalone-serve font 404 at `/site/fonts/InterVariable.woff2`, `c7ad11f`, Pages-owned, remained the only unexpected console 404.
- Historical residual: mechanics observatory category row put two elements past the right edge at `1440 px`, including `Advanced Guardrails` button to `1457 px`.
- Residual proof: category-row measurements matched the unmodified prototype, establishing pre-existence; the issue caused no document overflow.
- Receipts boundary: block remained snapshot-only with chip `no Host route yet`, because no Host route serves receipt kinds.
- Historical operator boundary: trace drawer's live branch required a Host route publishing a lane's trace identity before operators could reach it.
- Historical scope boundary: publishing that identity required a new AKASHA query, outside this packet's Rust boundary.
- Open authority gate: implementation and rendered evidence were complete; whether new strip wording earns footer space remained Sol's own squint-test verdict.

#### Proof receipt — 2026-09-05 lane spans: the operator-reachable trace id

- Repair scope: closes BUGS.md row `Pulse trace drawer has no operator-reachable trace id`.
- Prior proof boundary: drawer's live branch was proven last wave but unreachable because `insula.vitals_minute` rollup lanes carry no trace id.
- Prior route boundary: no Host route listed spans.
- Drawer stage one: clicking a lane asks new `/live/insula/spans` for that lane's newest settled spans.
- Drawer stage two: clicking a returned span opens the existing trace drawer for its `trace_id`.
- Architecture: one bounded AKASHA read, `insula.spans.recent v1`, implemented by `query_spans` in `crates/akasha/src/insula/query.rs`.
- Host route: new `/athanor/v1/insula/spans` shares the family's `require_bearer` middleware.
- Page route: POST allow-list entry `/live/insula/spans`.
- Window contract: `window` is closed set `15m` | `1h` | `24h`, not a caller-supplied interval.
- Window constraint: only a recent lower bound keeps the read an index walk.
- Room contract: `room` is required without wildcard, unlike `TraceScope::room`.
- Scope rationale: every returned `span_id`/`trace_id` is a working key into `insula/trace`; house-wide lane reads would let one room enumerate another's traces.
- Trusted binding: house and room both come from the Host; the body names only the lane.
- Index baseline: migration 0022 left nothing answering this query shape.
- Existing index: `idx_insula_log_vitals` leads on `observed_at`, making house/room/operation in-index filters rather than a search prefix.
- Existing index: `idx_insula_log_window` performs that walk without filtering, requiring a multi-million-row walk to return ten.
- Migration added: next numbered idempotent migration `0029_insula_log_lane_spans.sql`, indexing `(house_id, room, operation, observed_at DESC, span_id DESC)`.
- EXPLAIN setup: isolated schema, using shipped `SPANS_SQL` const itself so proof cannot drift from the statement.
- EXPLAIN result: `Limit → Index Scan using idx_insula_log_lane_spans on log`.
- EXPLAIN result: `Index Cond` carries all three equality columns plus `observed_at >= now() - interval`, with `phase` as a recheck filter.
- EXPLAIN result: **no Sort node**.
- Index exclusions: `phase`/`outcome_class` are deliberately absent because placing them before `observed_at` would break ordering for the unfiltered lane read performed every click.
- Proof-discovered defects: two real defects surfaced, one in SQL and one in the rendered surface.
- SQL defect: `ORDER BY span_id DESC` silently bound to `span_id::text` **output alias** rather than the uuid column.
- SQL defect proof: `Sort Key: ((span_id)::text) DESC` appeared, preventing the index from serving ordering.
- SQL fix: ordering now qualifies `log.observed_at, log.span_id`.
- Keyboard defect: activating a span returned focus to lane `<summary>`, stranding the operator above all eight rows with the newly opened trace drawer below.
- Keyboard fix: `renderWithSpanFocus` returns focus to the pressed span row.
- Keyboard proof: focus became `BUTTON {"span":"fde9e038-…"}` instead of `SUMMARY {"lane":"omp_adapter tool_call"}`.
- Historical setup: Chromium at `1440 × 1000` and `390 × 844`, LIVE against a real Insula router reading real PostgreSQL rows.
- Surface proof: all four states rendered at both viewports.
- List result: `8 spans`, `insula.spans.recent v1 · last 24h · queried 12:46 local · truncated at 8`.
- List rows: eight `<button>` rows, each carrying observed time, span id, trace id, duration, and outcome.
- List flags: `error`/`tool_error` appeared where carried by the row.
- Drill-down result: clicking newest row opened `Trace · 2 spans · insula.trace v1 · trace 1b2129bc-abd7-4b8e-bf9e-f60ac1c3f1ea`.
- Drill-down identity: `tool_call` span `31c240df-c2c0-4062-921d-e7e49a1cf001` was the exact span pressed, beside its child `provider_request`.
- Drill-down acceptance: displayed the same spans `insula/trace` returns for that id, satisfying the row's own acceptance.
- Empty-window result: `No spans in window`, `insula.spans.recent v1 answered zero settled spans for knock_claim in the last 24h inside this room's scope`.
- Refusal result: `Spans refused`, `Host refused: invalid_request`, `Requested lane Bad Lane.`.
- Refusal setup: genuine Host `422`, not injected, from a seeded rollup whose operation is not a mechanical atom.
- Refusal boundary: `query_spans` refused the operation before reaching the pool.
- Selection result: `aria-pressed` marks exactly the chosen row, `0:false 1:false 2:true …`.
- Viewport results: zero horizontal document overflow and zero elements past the right edge at both viewports; zero JavaScript exceptions.
- Deployment boundary: installed Host on `:8787` does not carry this route and could not answer it.
- Rendered-proof setup: throwaway harness on `:8799` served the **real** `InsulaHost` router, same handler, same bearer middleware, and same `query_spans`.
- Harness data: real rows in isolated `athanor_substrate_test` Insula schema.
- Prototype setup: second server on `:4187` targeted the harness through new `PULSE_HOST_PORT` override in `serve.ts`.
- Live-system boundary: live `athanor.exe`, live `:8787` Host, and `:4186` were never touched or stopped.
- Harness outcome: deleted after proof.
- BUGS status: **Repaired but not deployed**, with deployed-Host proof explicitly owed rather than claimed.
- Historical residual: standalone-serve font 404 at `/site/fonts/InterVariable.woff2`, Pages-owned, still appeared.
- Harness route results: `/live/health`, `/live/docket/board`, and `/live/hallway/inbox` returned 404 because the harness serves only the Insula family.
- Separate-channel result: footer honestly read `Host unreachable` while Pulse panel read `Host connected`, demonstrating the separate-channels rule, not a regression.
- Full-Host condition: deployed against a full Host, both channels go live together.
- Open authority gate: implementation and rendered evidence were complete.
- Open window verdict: whether a lane should default to `24h` rather than a shorter window remained Sol's own squint-test verdict.
- Open count verdict: whether eight rows is the right handful remained Sol's own squint-test verdict.

#### Proof receipt — 2026-09-05 mechanics category overflow

- Repair: `.mechanics-categories` now wraps at all widths.
- Repair: removed the old desktop horizontal scroll rule.
- Repair: the mobile override now applies only to `.shelf-marks`.
- Unchanged: chip spacing, borders, type, count badges, and markup.
- Historical browser setup: served the development checkout with `PULSE_PORT=4186 bun gui-prototype/serve.ts`.
- Historical browser setup: opened headless Chrome with `--remote-debugging-port=9346` and a separate proof profile.
- Historical browser setup: attached through CDP, used `page.setViewport`, opened House slot 2, and measured `getBoundingClientRect().right`.
- Historical mobile setup: waited for resize animations to finish before measurement.
- Measurement units: right edges in pixels.
- Measurement category order: All; Recall & Context; Memory, Lessons & Anamnesis; GIGA & Embeddings; Host, Delivery & Hallways; Rooms & Sessions; Backups; Advanced Guardrails.
- Historical viewport `1440 × 1000`:
  - button right edges were `408.4375, 541.03125, 743.9375, 896.140625, 1072.265625, 1213.328125, 1305.140625, 496.078125`.
  - count right edges were `398.4375, 531.03125, 733.9375, 886.140625, 1062.265625, 1203.328125, 1295.140625, 486.078125`.
  - all eight buttons fit inside the column's `1360 px` right edge.
  - the last chip wraps to the next line.
- Historical viewport `390 × 844`:
  - button right edges were `71.4375, 204.03125, 209.90625, 362.109375, 183.125, 324.1875, 98.8125, 250.890625`.
  - count right edges were `61.4375, 194.03125, 199.90625, 352.109375, 173.125, 314.1875, 88.8125, 240.890625`.
  - all eight buttons fit inside the column's `367 px` right edge.
- Historical results, both viewports:
  - all eight count rectangles fit fully inside their buttons.
  - the settled page had zero positive-size elements past the viewport's right edge.
  - the settled page had zero horizontal document overflow.
  - screenshots confirmed visible chips and counts.
- Historical viewport `390 × 844`: clicking Advanced Guardrails set its `aria-pressed` value to `true`.
- Proof scope: development browser surface and existing source-census categories.
- Proof boundary: no deployment, durable write, or live process stop occurred.
- Outstanding verification: the deployed surface still needs the same two-viewport check after deployment.

## Update rule

Keep this file a number-only retrieval index, not a substitute for PostgreSQL lesson bodies. Preserve all typed IDs, grouped and prose references, supplied titles, and routing. Retain untyped references without guessing a family. Give each lesson one retrieval line and one use clause; use `(title in registry)` when no title is supplied. Keep defaults in Always and omit empty query families. Record non-lesson facts as compact single-sentence schema entries under Repo facts without a lesson number, preserving every measurement and historical proof boundary. Keep the three-slot UI table verbatim. Refresh the date when the map changes and report unresolved titles or types.
