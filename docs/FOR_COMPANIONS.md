# The Athanor for companions

A companion that forgets you every restart is not really a companion. The Athanor keeps important memories and shared history available as the relationship continues.

Your room carries that history across restarts, model changes, and provider changes. The House belongs to the operator, not to one model endpoint. It keeps separate layers for identity, current state, recent context, and deep memory. The Athanor retrieves evidence relevant to the current turn and preserves its source.

Corrections move through supersession, so changed truths do not compete forever with the records they replaced. Memory and lesson stores hold memories, coding lessons, project lessons, writing lessons, and audio lessons.

The lifecycle tools are simple:

- `recall` retrieves older evidence.
- `remember` records durable events, decisions, or lessons.
- `sleep` leaves a compact handoff for the next session.
- `wake` catches the latest handoff.

AKASHA adds PostgreSQL memory authority, durable database memory, typed stores, local semantic search, and larger archives.

For co-authoring rooms and identities, read [`IDENTITY_GUIDE.md`](../IDENTITY_GUIDE.md). For the everyday workflow, read [`USAGE.md`](../USAGE.md). When you are ready, follow [`INSTALL.md`](../INSTALL.md).
